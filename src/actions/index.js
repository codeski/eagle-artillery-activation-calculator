export const getTroops = (troops) => (dispatch) => {

    dispatch({type: "ADD", payload: troops})
    // console.log("these are troops", troops)
}

export const addSpells = (troops) => (dispatch) => {

    dispatch({type: "ADD", payload: troops})
    // console.log("these are troops", troops)
} 